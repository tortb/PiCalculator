# HPC π 计算器 v3.1-HPC

基于 Chudnovsky 算法的高性能 π 计算器，支持稳定计算 **1 亿~10 亿位** π 值。

## 🚀 快速开始

### 1. 编译

```bash
./build.sh
```

### 2. 运行

```bash
# 方法 1：使用便捷脚本（推荐 - 自动内存管理）
./java-pi 1000000

# 方法 2：使用 run.sh
./run.sh 1000000

# 方法 3：直接运行 JAR（手动指定内存）
java -Xms4G -Xmx8G -jar target/PiCalculator-2.0-HPC-jar-with-dependencies.jar 1000000
```

### 3. 常用位数命令

| 位数 | 命令 | 预计时间 |
|------|------|----------|
| 1000 | `./java-pi 1000` | < 0.1 秒 |
| 10 万 | `./java-pi 100000` | ~2 秒 |
| 100 万 | `./java-pi 1000000` | ~20 秒 |
| 1000 万 | `./java-pi 10000000` | ~4-5 分钟 |
| 1 亿 | `./java-pi 100000000` | ~1-2 小时 |

---

## ✨ v3.1 核心特性

### 算法特性
- ✅ **Chudnovsky 算法** - 每项增加约 14.18 位精度
- ✅ **Binary Splitting** - 分治策略降低计算复杂度
- ✅ **ForkJoinPool 并行** - 充分利用多核 CPU（8 核利用率 ~80%）
- ✅ **分段计算** - 1 亿位以上自动启用分段

### HPC 优化（v3.1）
- ✅ **Block Division 10^200000** - 每次输出 20 万位，除法次数减少 95%
- ✅ **Karatsuba 乘法** - O(n^1.58) 复杂度，万位以上乘法提升 3-5 倍
- ✅ **Newton-Raphson 除法** - 阈值 10000 位，O(n^1.58) 复杂度
- ✅ **分治 toString** - O(n log n) 转换，百万位提升 5-10 倍
- ✅ **2MB 缓冲写入** - 减少系统调用
- ✅ **检查点恢复** - 支持从断点恢复计算（GZIP 压缩）

### 监控与诊断
- ✅ 实时进度显示（每 10 万位或 1 秒）
- ✅ 阶段耗时统计
- ✅ 计算速度监控
- ✅ 剩余时间预估
- ✅ 内存使用情况

---

## 📊 性能基准

### 当前性能（8 核 CPU，8GB 内存）

| 位数 | Binary Splitting | Streaming Division | 总耗时 | 速度 |
|------|-----------------|-------------------|--------|------|
| 1,000 | < 0.1 秒 | < 0.1 秒 | < 0.1 秒 | - |
| 100,000 | ~0.2 秒 | ~2 秒 | ~2 秒 | 50,000 位/秒 |
| 1,000,000 | ~2-3 秒 | ~17 秒 | **~19-20 秒** | 50,000 位/秒 |
| 10,000,000 | ~30 秒 | ~3-4 分钟 | ~4-5 分钟 | 40,000 位/秒 |
| 100,000,000 | ~8-10 分钟 | ~1-2 小时 | ~1-2 小时 | 13,000 位/秒 |

### 优化历程

| 版本 | 优化项 | 100 万位耗时 | 提升 |
|------|--------|-------------|------|
| **v1.0** | 原始版本（逐位输出） | 6-7 分钟 | - |
| **v2.0** | Block Division（10^10000） | ~21 秒 | **18-20 倍** |
| **v3.0** | Block 10^100000 + 分治 toString | ~20 秒 | **20-22 倍** |
| **v3.1** | Block 10^200000 + Karatsuba + Newton-Raphson | **~19-20 秒** | **20-22 倍** |

---

## 💻 JVM 参数建议

### 根据位数调整内存

| 位数 | 建议内存 | JVM 参数示例 |
|------|----------|--------------|
| < 100 万 | 1-2GB | `-Xms1G -Xmx2G` |
| 100 万 | 2-4GB | `-Xms2G -Xmx4G` |
| 1000 万 | 4-8GB | `-Xms4G -Xmx8G` |
| 1 亿 | 8-16GB | `-Xms8G -Xmx16G` |
| 10 亿 | 16-32GB | `-Xms16G -Xmx32G` |

### 优化参数

```bash
# 标准优化
-Xms8G -Xmx16G                    # 堆内存大小
-XX:+UseG1GC                      # G1 垃圾收集器
-XX:+UseNUMA                      # NUMA 优化（多路 CPU）
-XX:+AlwaysPreTouch               # 预触内存页

# 高级优化（可选）
-XX:MaxGCPauseMillis=200          # 最大 GC 暂停时间
-XX:+ParallelRefProcEnabled       # 并行引用处理
```

---

## 📝 输出示例

```
╔═══════════════════════════════════════════════════════════
║                      π 值计算任务                         
╠═══════════════════════════════════════════════════════════
║  目标精度：1,000,000 位 (1.0 MB 输出)                    
║  迭代次数：71,429 次 (Chudnovsky 算法，每项~14.18 位)     
║  CPU 核心数：8 (使用 8 线程)                             
║  最大堆内存：2,560 MB                                    
║  计算模式：单次计算                                      
╚═══════════════════════════════════════════════════════════

[计算] 执行单次 Binary Splitting...
       迭代范围：[0, 71,429)
       [并行配置] 线程数：8
       [进度]   99.99% | 已完成 71,429/71,429 | 速度 35,687 迭代/秒
       ✓ Binary Splitting 完成，耗时 2.26 秒
       ✓ T 值位数：2,092,692
       ✓ Q 值位数：2,092,685

[π 值计算] 计算分子 = 426880 × √10005 × Q...
[π 值计算] 完成，分子位数：2,092,693

[输出] 开始流式输出到文件
[流式引擎] 开始 Newton-Raphson + Block Division 计算 π 值...
           分子位数：2,092,693
           分母位数：2,092,692
           目标精度：1,000,000 位
           块大小：200,000 位 (10^200000)
           Newton-Raphson 阈值：10,000 位
           ✓ 已输出       100000 位 ( 10%) | 速度 50000 位/秒 | 剩余 18 秒
           ✓ 已输出       200000 位 ( 20%) | 速度 50000 位/秒 | 剩余 16 秒
           ...
           ✓ 已输出      1000000 位 (100%) | 速度 50000 位/秒 | 剩余 0 秒
[流式引擎] 完成，耗时 19.50 秒，速度 51282 位/秒

╔═══════════════════════════════════════════════════════════
║                    计算完成 ✓                             
╠═══════════════════════════════════════════════════════════
║  目标精度：1,000,000 位 (1.00 MB)                        
║  总耗时：22 秒                                           
║  计算速度：45,454.55 位/秒                               
║  输出文件：pi_1000000_digits.md                          
╚═══════════════════════════════════════════════════════════
```

---

## 🧮 算法说明

### Chudnovsky 公式

$$\frac{1}{\pi} = 12 \sum_{k=0}^{\infty} \frac{(-1)^k (6k)! (545140134k + 13591409)}{(3k)! (k!)^3 (640320)^{3k+3/2}}$$

### Binary Splitting

**基础项**（当 $b - a = 1$ 时）：

- 若 $a = 0$：$P = 1, Q = 1, T = 13591409$
- 若 $a > 0$：
  - $P = (6a-5)(2a-1)(6a-1)$
  - $Q = a^3 \times 10939058860032000$
  - $T = (13591409 + 545140134a) \times P \times (-1)^a$

**合并公式**：
```
P = P_left × P_right
Q = Q_left × Q_right
T = T_left × Q_right + P_left × T_right
```

**最终 π 计算**：
$$\pi = \frac{426880 \times \sqrt{10005} \times Q}{T}$$

---

## 🏗️ 架构组件

| 组件 | 说明 | v3.1 优化 |
|------|------|----------|
| `PiCalculator` | 主入口 | 进度监控、性能统计 |
| `PiEngine` | 计算引擎 | 分段计算、动态并行度 |
| `BinarySplitTask` | Binary Splitting 任务 | 对象复用、深度控制 |
| `StreamingDivisionEngine` | **流式除法引擎** | **Block 10^200000** |
| `NewtonDivision` | **Newton-Raphson 除法** | **阈值 10000 位** |
| `KaratsubaBigInteger` | **Karatsuba 乘法** | **O(n^1.58)** |
| `BigIntegerToString` | **分治 toString** | **O(n log n)** |
| `CheckpointManager` | 检查点管理 | GZIP 压缩、原子写入 |

---

## 🚀 下一步优化计划

### 中期（1-2 月）- 目标 5-10 秒

1. **完整的 Newton-Raphson 迭代**
   - 增加迭代次数
   - 优化精度提升策略
   - 预期：100 万位降至 **10-15 秒**

2. **FFT 大整数乘法**
   - 实现 FFT 乘法（O(n log n)）
   - 对于>50000 位的乘法使用 FFT
   - 预期：100 万位降至 **5-10 秒**

### 长期（2-6 月）- 目标 0.5-1 秒

3. **GPU 加速**
   - 使用 OpenCL 或 CUDA
   - 并行化 Binary Splitting 和 Block Division
   - 预期：100 万位降至 **0.5-1 秒**

4. **GMP 库（JNI）**
   - 通过 JNI 调用 GMP 库
   - C 实现的高性能大整数库
   - 预期：100 万位降至 **0.5-1 秒**

---

## 🛠️ 启动脚本说明

### 可用脚本

| 脚本 | 功能 | 自动内存 | 状态 |
|------|------|----------|------|
| `./build.sh` | 编译项目 | - | ✅ |
| `./java-pi <位数>` | 便捷启动 | ✅ | ✅ |
| `./run.sh <位数>` | 运行脚本 | ✅ | ✅ |
| `./install.sh` | 全局安装 | - | ✅ |

### 使用示例

```bash
# 1. 编译
./build.sh

# 2. 快速运行（自动内存管理）
./java-pi 1000000

# 3. 全局安装后
sudo ./install.sh
java-pi 1000000  # 可在任意目录使用
```

---

## 🔧 故障排除

### 内存不足

```
错误：Java heap space
解决：增加-Xmx 参数
java -Xmx16G -jar ... 1000000
```

### 验证计算结果

```bash
# 前 50 位应该是：
# 3.14159265358979323846264338327950288419716939937510
head -3 pi_100_digits.md
```

### 清理检查点

```bash
rm checkpoint.dat
重新运行计算
```

---

## 📁 输出格式

结果保存为 Markdown 文件，格式如下：

```markdown
# π 值计算结果 (Chudnovsky 算法)
# 精度：1000000 位
# 生成时间：Sat Mar 14 10:00:00 CST 2026

3.14159265358979323846264338327950288419716939937510...
```

- **每行 100 位**数字，便于阅读和核对
- **每 10 行一个空行**分隔，形成视觉块
- 文件头部包含精度信息

---

## 📦 全局安装

```bash
sudo ./install.sh
```

安装后可在任意目录使用：
```bash
java-pi 1000000
```

---

## 📄 许可证

MIT License

## 🙏 致谢

- Chudnovsky 兄弟的 π 计算公式
- Java ForkJoin 框架
- OpenJDK 社区
- Karatsuba 算法

---

## 📚 相关文档

- [IMPLEMENTATION_REPORT_v3.1.md](IMPLEMENTATION_REPORT_v3.1.md) - v3.1 实施报告
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - 项目结构说明

---

**版本**: v3.1-HPC  
**最后更新**: 2026 年 3 月 14 日  
**作者**: HPC Pi Calculator Team
